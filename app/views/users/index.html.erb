<% provide(:title, 'All users') %>
<div class="container">
	<div class="row">
	<h1>All users</h1>
	<ul class="users">
		<% @users.each do |user| %>
  		<%= render @users %>
  		<% end %>
  	</ul>
  	<%= will_paginate @users%>
	</div>
</div>

